services:
  extractor:
    build: .
    network_mode: "host"
    container_name: ai-de-s-container
    shm_size: '2gb'
    deploy:
      resources:
        limits:
          memory: 4G
    env_file: .env
    volumes:
      - ./data/output:/app/data/output
    restart: "no" 